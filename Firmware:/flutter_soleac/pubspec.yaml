name: soleac_ble_app
description: App Flutter para leer la telemetria BLE del detector SOLEAC.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.3.1
  permission_handler: 12.0.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
